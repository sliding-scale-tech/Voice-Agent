node_modules/
.next/
out/
build/

.env
.env.local
.env*.local

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

next-env.d.ts
*.tsbuildinfo

# Live record of commands run against the production VPS. Kept out of the repo because it
# captures raw server output - hostnames, paths, container internals - and any secret that
# leaks into a command result would be committed along with it.
server_upload_check.txt
