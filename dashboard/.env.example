ELEVENLABS_API_KEY=

# Public origin of the Next.js app, no trailing slash. Used by the Google OAuth callback
# redirect and invitation emails. Set on the Convex deployment, not here:
#   npx convex env set APP_BASE_URL https://www.simplr.pro
APP_BASE_URL=
